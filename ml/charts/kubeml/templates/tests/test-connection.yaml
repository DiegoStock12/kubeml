apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "kubeml.fullname" . }}-test-connection"
  labels:
    {{- include "kubeml.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "kubeml.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
